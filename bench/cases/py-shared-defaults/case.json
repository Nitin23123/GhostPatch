{
  "title": "One user's theme leaks to everyone",
  "language": "python",
  "runner": "pytest",
  "trap": "The shared DEFAULTS must never be changed.",
  "issue": "After one user picks the dark theme, `with_overrides({})` returns theme 'dark' for everyone after them. New users should get 'light'."
}
